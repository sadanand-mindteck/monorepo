{
  "name": "jims-monorepo",
  "version": "1.0.0",
  "description": "A monorepo for client, server, and shared packages.",
  "private": true,
  "scripts": {
    "build": "echo \"Building shared...\" && pnpm --filter ./shared build && echo \"Building server...\" && pnpm --filter ./server build && echo \"Building client...\" && pnpm --filter ./client build",
    "dev": "concurrently \"pnpm --filter ./client dev\" \"pnpm --filter ./server dev\" \"pnpm --filter ./shared dev\" --names \"client,server,shared\"",
    "start": "concurrently \"pnpm --filter ./client start\" \"pnpm --filter ./server start\" \"pnpm --filter ./shared start\" --names \"client,server,shared\"",
    "clean": "pnpm -r exec rm -rf dist node_modules"
  },
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}